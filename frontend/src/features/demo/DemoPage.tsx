export default function DemoPage() {
    return (
      <div className="min-h-screen flex items-center justify-center bg-if-blue text-white text-4xl font-bold">
        Hello If World
      </div>
    );
}
