import { describe, test } from 'vitest';

// TODO: Add proper tests for ProductCard
// Make sure it doesn’t break when a product is missing image or price
// and that it gracefully handles long text too. Future JG, this one's yours.

describe('ProductCard', () => {
  test.todo('renders title, description, price, image — the whole package');
});
